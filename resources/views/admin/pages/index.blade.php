@extends('layouts.admin') @section('title','Páginas') @section('heading','Páginas informativas')
@section('content')<div class="admin-actions"><p>Administra información legal, envíos, devoluciones, contacto y la historia de la marca.</p><a class="button button-dark" href="{{ route('admin.pages.create') }}">Nueva página</a></div><section class="admin-panel table-wrap"><table><thead><tr><th>Página</th><th>URL</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($pages as $page)<tr><td data-label="Página"><strong>{{ $page->title }}</strong></td><td data-label="URL">/paginas/{{ $page->slug }}</td><td data-label="Estado"><span class="status {{ $page->is_active ? 'success':'' }}">{{ $page->is_active ? 'Publicada':'Borrador' }}</span></td><td data-label="Acciones" class="row-actions"><a href="{{ route('admin.pages.edit',$page) }}">Editar</a><form method="POST" action="{{ route('admin.pages.destroy',$page) }}" data-confirm="¿Eliminar esta página?">@csrf @method('DELETE')<button>Eliminar</button></form></td></tr>@empty<tr><td colspan="4">Aún no hay páginas.</td></tr>@endforelse</tbody></table></section>{{ $pages->links() }}@endsection

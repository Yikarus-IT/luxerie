@extends('layouts.admin') @section('title',$page->exists?'Editar página':'Nueva página') @section('heading',$page->exists?'Editar página':'Nueva página')
@section('content')<form class="admin-form" method="POST" action="{{ $page->exists?route('admin.pages.update',$page):route('admin.pages.store') }}">@csrf @if($page->exists)@method('PUT')@endif<section class="form-panel"><div class="form-grid"><label class="span-2">Título<input name="title" value="{{ old('title',$page->title) }}" required></label><label>Slug<input name="slug" value="{{ old('slug',$page->slug) }}" placeholder="Se genera desde el título"></label><label class="check"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$page->is_active))> Publicada</label><label class="span-2">Resumen<textarea name="excerpt" rows="2" maxlength="300">{{ old('excerpt',$page->excerpt) }}</textarea></label><label class="span-2">Contenido<textarea name="body" rows="14" required>{{ old('body',$page->body) }}</textarea></label><label>Título SEO<input name="seo_title" maxlength="70" value="{{ old('seo_title',$page->seo_title) }}"></label><label>Descripción SEO<textarea name="seo_description" maxlength="160" rows="3">{{ old('seo_description',$page->seo_description) }}</textarea></label></div></section><div class="form-actions"><a href="{{ route('admin.pages.index') }}">Cancelar</a><button class="button button-dark">Guardar página</button></div></form>@endsection

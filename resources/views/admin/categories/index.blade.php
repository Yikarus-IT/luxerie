@extends('layouts.admin')
@section('title', 'Categorías')
@section('heading', 'Categorías')
@section('content')
<div class="category-layout"><form class="form-panel compact-form" method="POST" action="{{ route('admin.categories.store') }}">@csrf<p class="eyebrow">Nueva colección</p><h2>Agregar categoría</h2><label>Nombre<input name="name" required></label><label>Slug<input name="slug" placeholder="Se genera a partir del nombre"></label><label>Descripción<textarea name="description" rows="4"></textarea></label><label class="check"><input type="checkbox" name="is_active" value="1" checked> Visible</label><button class="button button-dark button-full" type="submit">Agregar categoría</button></form><section class="admin-panel"><div class="panel-heading"><div><p class="eyebrow">Estructura del catálogo</p><h2>Categorías existentes</h2></div></div>@foreach($categories as $category)<form class="category-row" method="POST" action="{{ route('admin.categories.update', $category) }}">@csrf @method('PUT')<input name="name" value="{{ $category->name }}" aria-label="Nombre de categoría"><input name="slug" value="{{ $category->slug }}" aria-label="Slug de categoría"><span>{{ $category->products_count }} productos</span><label class="check"><input type="checkbox" name="is_active" value="1" @checked($category->is_active)> Visible</label><button type="submit">Guardar</button></form>@endforeach</section></div>
@endsection

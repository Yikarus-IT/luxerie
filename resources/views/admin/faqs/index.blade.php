@extends('layouts.admin') @section('title','Preguntas frecuentes') @section('heading','Preguntas frecuentes')
@section('content')<div class="management-grid"><form class="form-panel compact-form" method="POST" action="{{ route('admin.faqs.store') }}">@csrf<h2>Nueva pregunta</h2><label>Pregunta<input name="question" required></label><label>Respuesta<textarea name="answer" rows="5" required></textarea></label><label>Orden<input type="number" name="sort_order" value="0" min="0" required></label><label class="check"><input type="checkbox" name="is_active" value="1" checked> Publicada</label><button class="button button-dark">Agregar</button></form><section class="admin-panel"><h2>Preguntas actuales</h2>@foreach($faqs as $faq)<form class="management-row" method="POST" action="{{ route('admin.faqs.update',$faq) }}">@csrf @method('PUT')<label>Pregunta<input name="question" value="{{ $faq->question }}" required></label><label>Respuesta<textarea name="answer" rows="3" required>{{ $faq->answer }}</textarea></label><label>Orden<input type="number" name="sort_order" value="{{ $faq->sort_order }}" min="0"></label><label class="check"><input type="checkbox" name="is_active" value="1" @checked($faq->is_active)> Publicada</label><button class="button button-small">Guardar</button><button class="text-link" type="submit" form="delete-faq-{{ $faq->id }}">Eliminar</button></form><form id="delete-faq-{{ $faq->id }}" method="POST" action="{{ route('admin.faqs.destroy',$faq) }}" data-confirm="¿Eliminar esta pregunta?">@csrf @method('DELETE')</form>@endforeach</section></div>@endsection

@extends('layouts.admin')
@section('title', $order->number) @section('heading', 'Pedido '.$order->number)
@section('content')
<div class="admin-two-column"><section class="form-panel"><h2>Productos</h2><div class="table-wrap"><table class="admin-table"><thead><tr><th>Producto</th><th>Cantidad</th><th>Precio</th><th>Total</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->name }}<br><small>{{ $item->sku }}</small></td><td>{{ $item->quantity }}</td><td>${{ number_format($item->unit_price,2) }}</td><td>${{ number_format($item->line_total,2) }}</td></tr>@endforeach</tbody></table></div><p><strong>Total: ${{ number_format($order->total,2) }} {{ $order->currency }}</strong></p><h2>Cliente y envío</h2><p>{{ $order->customer_name }} · {{ $order->customer_email }} · {{ $order->customer_phone }}</p><p>{{ $order->shipping_address_line_1 }} {{ $order->shipping_address_line_2 }}, {{ $order->shipping_neighborhood }}, {{ $order->shipping_city }}, {{ $order->shipping_state }}, C.P. {{ $order->shipping_postal_code }}</p></section><aside><section class="form-panel"><h2>Actualizar estado</h2><form method="POST" action="{{ route('admin.orders.update',$order) }}">@csrf @method('PATCH')<label>Estado<select name="status">@foreach(['pending_payment','paid','processing','shipped','delivered','canceled'] as $status)<option @selected($order->status===$status)>{{ $status }}</option>@endforeach</select></label><label>Comentario<textarea name="comment" rows="3"></textarea></label><button class="button button-dark" type="submit">Guardar estado</button></form></section><section class="form-panel"><h2>Historial</h2>@foreach($order->statusHistory->sortByDesc('created_at') as $entry)<p><strong>{{ $entry->status }}</strong><br><small>{{ $entry->created_at->format('d/m/Y H:i') }}{{ $entry->user ? ' · '.$entry->user->name : '' }}</small><br>{{ $entry->comment }}</p>@endforeach</section></aside></div>
@endsection
